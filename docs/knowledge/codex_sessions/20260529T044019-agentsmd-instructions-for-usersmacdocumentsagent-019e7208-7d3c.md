# Codex Session: # AGENTS.md instructions for /Users/mac/Documents/agent

<!-- Generated by scripts/import_codex_sessions.py. Do not edit by hand. -->

- Session ID: `019e7208-7d3c-7e71-aa84-c2542a3e9796`
- Timestamp: `2026-05-29T04:40:19.565Z`
- Source: `/Users/mac/.codex/sessions/2026/05/29/rollout-2026-05-29T12-40-19-019e7208-7d3c-7e71-aa84-c2542a3e9796.jsonl`
- CWD: `/Users/mac/Documents/agent`

## Transcript

### User

# AGENTS.md instructions for /Users/mac/Documents/agent

<INSTRUCTIONS>
# Nora Codex Collaboration Rules

Codex is the project manager, reviewer, and committer for this repository. Codex windows are implementation workers.

## Worker Identity

At the start of a Codex window, identify yourself as either:

- Codex A
- Codex B

Then follow only the matching task file:

- Codex A reads `agent_tasks/A_TASK.md`
- Codex B reads `agent_tasks/B_TASK.md`

## Task Flow

0. Read `docs/knowledge/PROJECT_WAKEUP.md`, `docs/knowledge/DECISIONS.md`, and `docs/knowledge/CHAT_INDEX.md` so you inherit the project context from prior windows.
1. Read your assigned task file.
2. Inspect the current git worktree before editing.
3. Implement only your assigned scope.
4. Run the required checks listed in your task file.
5. Write your completion report:
   - Codex A writes `agent_tasks/A_DONE.md`
   - Codex B writes `agent_tasks/B_DONE.md`
6. Notify Codex PM:
   - Codex A runs `agent_tasks/notify_codex.sh A`
   - Codex B runs `agent_tasks/notify_codex.sh B`
7. Do not push.
8. Do not commit unless Codex explicitly asks.

## Boundaries

- Do not edit the other worker's task or done file.
- Do not edit `CODEX_TERMINAL_HANDOFF.md`.
- Do not edit `designs/` unless explicitly assigned.
- Avoid broad refactors while another worker is active.
- If your task conflicts with uncommitted work from another worker, stop and write the conflict in your DONE file.

## Completion Report Format

Use this structure:

```markdown
# Codex A/B Completion Report

Status: ready for Codex review

## Summary

...

## Diff

```text
<git diff --stat output>
```

## Tests

```text
<exact commands and pass/fail results>
```

## Notes

- No push performed.
- Known issues: ...
```

## Git Safety

Never run destructive git commands such as:

- `git reset --hard`
- `git checkout -- .`
- `git clean -fd`
- `git push`
- force push commands

Codex owns final review, commit, and push decisions.

</INSTRUCTIONS>
<environment_context>
  <cwd>/Users/mac/Documents/agent</cwd>
  <shell>zsh</shell>
  <current_date>2026-05-29</current_date>
  <timezone>Asia/Shanghai</timezone>
</environment_context>

### User

Code review side task: In /Users/mac/Documents/agent, review the diff `git diff origin/main...HEAD` against documented repo standards. Read AGENTS.md, CLAUDE.md, and relevant docs/knowledge files only as needed. Report concrete findings only, with file/line references where possible. Focus on bugs, regressions, missing tests, or violations of documented standards. Do not edit files. Keep under 500 words.

### Assistant

I’ll use the `review` skill for this turn and keep this read-only. I’m going to inspect the documented standards first, then review `origin/main...HEAD` for concrete issues only.

### Assistant

The repo has both `AGENTS.md` and `CLAUDE.md`, plus the durable knowledge docs. I’m narrowing the standards to what applies to the changed files and then I’ll inspect the actual hunks.

### Assistant

The changed surface is durable tasks, runner/CLI wiring, evals, and task handoff docs. I’m reading the durable-runtime notes and then checking the Python hunks with line numbers so findings can be pinned precisely.

### Assistant

I’ve got enough standards context: this branch is supposed to advance durable runtime semantics with traceable, recoverable state, and the repo asks workers to stay within assigned scope and report checks. I’m now checking for behavioral regressions rather than style-only comments.

### Assistant

One probable standards/spec mismatch is emerging around default runtime wiring versus registry-only construction. I’m checking `build_agent()` now because the worker task explicitly names it as the integration point.

### Assistant

I’ve found multiple concrete issues in the durable-task path. I’m doing one last pass over the exact line references so the review stays tight and actionable.

### Assistant

**Findings**

1. [mini_agent/toolkits/registry_builder.py](/Users/mac/Documents/agent/mini_agent/toolkits/registry_builder.py:82): `TaskManager` is given a `DurableTaskStore`, but `enable_durable_shadow=True` is never passed. Since [mini_agent/task_runner.py](/Users/mac/Documents/agent/mini_agent/task_runner.py:15) defaults it to `False`, normal CLI/HTTP task operations still do not persist into durable tasks. This directly misses [agent_tasks/A_TASK.md](/Users/mac/Documents/agent/agent_tasks/A_TASK.md:14), which requires runtime wiring so “all CLI/HTTP runs persist tasks automatically.”

2. [mini_agent/task_runner.py](/Users/mac/Documents/agent/mini_agent/task_runner.py:182): `run_once()` marks a step `in_progress` and writes only the legacy task, but never calls `_shadow_sync_to_durable()`. Even if shadow sync is enabled, advancing execution leaves the durable record stale until a later update. This violates the durable lifecycle in [docs/knowledge/DURABLE_TASK_SCHEMA.md](/Users/mac/Documents/agent/docs/knowledge/DURABLE_TASK_SCHEMA.md:109), where execution should transition the task to `running`, point `current_step` at the active step, and accumulate trace linkage.

3. [mini_agent/durable_tasks.py](/Users/mac/Documents/agent/mini_agent/durable_tasks.py:688): `task_manager_task_to_durable()` never derives `current_step` from an `in_progress` legacy step, so durable records can show `status=running` with `current_step=None`. That contradicts [docs/knowledge/DURABLE_TASK_SCHEMA.md](/Users/mac/Documents/agent/docs/knowledge/DURABLE_TASK_SCHEMA.md:25) and the execute lifecycle at line 112. The added tests cover status mapping but not this required field.

4. [mini_agent/durable_tasks.py](/Users/mac/Documents/agent/mini_agent/durable_tasks.py:47): the branch makes `failed -> pending` valid and `retry_durable_task()` resets failed tasks to pending at [mini_agent/durable_tasks.py](/Users/mac/Documents/agent/mini_agent/durable_tasks.py:336). The documented schema still says `failed` is terminal in [docs/knowledge/DURABLE_TASK_SCHEMA.md](/Users/mac/Documents/agent/docs/knowledge/DURABLE_TASK_SCHEMA.md:57), and defines `failed` as “unrecoverable” at line 45. Either the spec needs updating with retry semantics or this is a standards violation.
